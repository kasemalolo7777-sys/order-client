export const controlState = {
    ShareModelOpen: false,
    shareModelInfo: {
        link:null,
    },
    pathQueue: [window.location.href], // Start with current URL
  currentIndex: 0,
  isOpenSidebar:true,
}